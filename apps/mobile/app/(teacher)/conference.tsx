// Teacher shell — Conference Hub stack route. ADR-102
// (docs/decisions/adr-102-teacher-shell-ia.md) demotes Conferences from a tab
// to a stack route reachable from Home; the screen is unchanged.
// SOT-KEYWORDS: teacher conference stack route mobile

import { ConferenceHubScreen } from '@acme/app';

export default ConferenceHubScreen;
