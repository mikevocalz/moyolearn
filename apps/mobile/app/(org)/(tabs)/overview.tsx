// The org.overview tab. OpsScreen is the SLIMMED overview now — greeting,
// today's sessions, the month's numbers — because the CRM (pipeline table,
// records, families, enrollment) split to the web-only /leads·/families·
// /enrollment routes (doc 36 §3.4; the mobile companion is
// Overview·Schedule·Inbox·Safety only). This tab used to render the entire
// web CRM, chrome and all — the org.overview defect.
import { OpsScreen } from "@acme/app";

export default OpsScreen;
