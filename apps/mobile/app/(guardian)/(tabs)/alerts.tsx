// Alerts tab — guardian.alerts: incidents + acknowledgments, never general
// notifications (the NotificationsScreen alias this replaces was the
// contract's named defect: Alerts ≠ Messages/Notifications, doc 36 §3.2).
import { GuardianAlertsScreen } from "@acme/app";

export default GuardianAlertsScreen;
