// /report-queue — doc 34 §5's Cool surface: the tutor draft-review queue and
// report trail. Deliberately NOT under /ops: doc 34 §3 walls summaries off
// from the CRM (`tooling/check-crm-wall.mjs`), so the queue lives beside the
// guardian surfaces and gates on the staff `write` capability server-side.
// SOT: docs/pack/34-session-summary-reports.md §5 · docs/pack/23-crm-spec.md §2
// SOT-KEYWORDS: report queue page route drafts cool tutor staff
import { SummaryQueueScreen } from '@acme/app';

export default SummaryQueueScreen;
