// TS resolution anchor — bundlers load the .native/.web forks.
// SOT-KEYWORDS: permission request anchor platform fork ts resolution native web
export { usePermissionRequester } from './permission-request.web';
