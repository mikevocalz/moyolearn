// TS resolution anchor — bundlers load the .native/.web forks.
// SOT-KEYWORDS: permissions storage anchor platform fork ts resolution native web
export { permissionsStateStorage } from './permissions-storage.web';
