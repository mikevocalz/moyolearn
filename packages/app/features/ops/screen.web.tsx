'use client';
// TS/platform fork — the ops surface is identical on both, so the shared file
// holds all of it. The forks exist because the generator's shape is the pattern.
export { OpsScreen } from './screen.shared';
