// Shared props for the PaneDivider platform forks.
// SOT: ./PaneDivider.native.tsx (drag) · ./PaneDivider.web.tsx (keyboard only)
// SOT-KEYWORDS: pane divider props resize width types
export interface PaneDividerProps {
  /** Current rendered width of the pane this divider resizes, in dp. */
  width: number;
}
