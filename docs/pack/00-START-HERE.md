# Moyo — the learning platform pack
**Moyo** (Swahili: *heart*) · tagline: **"Learn it by heart."** · brand spec: doc 02, Addendum B (name pending final trademark clearance)

**Mike W. Allen · Aug 19, 2026**

The mission in one line: help children get a better education — by opening the door 85% of students never open, and by making the humans around each child (parents, tutors, teachers) measurably better at helping them. Every document in this pack exists to serve that line; the privacy invariants aren't compliance overhead, they're child-outcome features.

## Reading order

1. **`01-ai-tutoring-platform-plan.md`** — market research, competitive teardown (Khanmigo's 15% engagement gap is the wedge), pricing, COPPA/FERPA constraints, ADRs (Supabase/Neon, Better Auth, Payload, Stripe, AI boundary), five role shells, screen inventory, roadmap, and the §9 agent-prompt roster + anti-slop gates.
2. **`02-adaptive-screens-design-spec.md`** — the Noto teardown, width classes, the popover→sheet→pane disclosure ladder, screen archetypes, the Neubrutalism × Swiss "Schoolhouse" language, motion spec, verification gates.
3. **`03-starter-tailoring.md`** — the two docs above corrected against your real repo: what already exists (a lot), what changes (Payload 4 canary stays, @acme/ui is the foundation, your window classes and snap points win), the dependency delta, and Phase 0 as seven ordered PRs.
4. **`04-screen-briefs.md`** — every critical screen with Job → Research → Layout → Design → Copy → A11y → Metric. The rule: no screen ships without a filled brief; a screen that can't cite a research finding hasn't been earned.

5. **`05-monetization-access-spec.md`** — pricing/trial research (hard-paywall and trial-LTV data, the post-click-to-cancel legal floor), the 30-day trial + paywall spec per shell, role routing with `Stack.Protected` + the visibility matrix, the Payload internal back office (founder + support), and the two-phase Stripe Connect rail: destination charges + payroll v1, then separate charges & transfers paying tutors directly.

6. **`06-auth-onboarding-spec.md`** — the account model (guardian-created child logins with username-only credentials, grant-access controls, guardian-only resets), verified Better Auth × Stripe wiring (referenceId per family/org, 30-day trials with built-in abuse prevention), COPPA-consent ladder, and the S21–S25 onboarding sequences per profile type.

7. **`07-security-spec.md`** — the device-first security pass: SecureStore policy (2KB rule, biometric-invalidation edges, THIS_DEVICE_ONLY), encrypted MMKV, the parent gate for shared family devices, EAS Update code signing, EXPO_PUBLIC_ discipline, server/data additions, the COPPA written-security-program mapping, and the per-PR security review gate.

8. **`08-visual-hierarchy-spacing-spec.md`** — the research-backed spacing tiers and age-band touch-target tokens (WCAG 2.2 floor → NN/g 2cm child targets), the UI type ramp the token file was missing, the neubrutalist hierarchy law (size/weight/space carry emphasis; borders are structure), component anatomy specs with exact tokens, and the imagery policy with the Pexels mood board.

9. **`09-screens-first-build-order.md`** — **the build order of record:** screens before auth. The Mock-Session Contract (one session interface, mock + live implementations, dev RoleSwitcher with age-band personas) that makes the deferral a provider swap instead of a rewrite, the five-wave roadmap, the guided-path Student Home research, and the per-screen definition of done.

10. **`10-types-components-spec.md`** — the typing contract (one strict tsconfig base, `any` banned, discriminated-union prop APIs, generated domain types, `as const satisfies` tokens) and the responsive reusable-component architecture (one `WidthClass` vocabulary, the CSS-before-JS responsive ladder, the kit contract, typed layout primitives). Read before writing any component in Wave 1/2.

11. **`11-architectural-guardrails.md`** + **`CLAUDE.md`** + **`PROMPTS.md`** — guardrail-coding methodology on the existing stack — **no tRPC, no new dependencies**. The `pnpm gen domain` generator already emits the layered architecture; this doc adds the shared block primitive, the permissions registry, lint rules that turn the generator's header comments into build errors, relationship scoping (identity is never a parameter — the child-safety form of tenant scoping), and the anti-slop/anti-forgetting discipline. `CLAUDE.md` and `PROMPTS.md` drop into the repo as-is.

12. **`12-systems-design-prompt.md`** — the systems-design prompt of record (roster-framed per the standing rule): binding high-level architecture (one server core around the Block, one Postgres with schema-level three-store separation, inference gateway as sole model egress), the five binding flows including the fail-closed learner AI turn, pg-boss/caching/SSE decisions with explicit trade-offs, the token-cost scale model, and the agent's deliverables. Where docs disagree, this one wins.

13. **`13-public-api-spec.md`** — the public API (Operations Cloud surface), studied from Noto's live spec and exceeded: registry-derived errors/scopes/docs/SDK, idempotency on all writes, signed webhooks, opaque IDs, sandbox powered by the persona fixtures, and the child-safety line — learner learning data has no public endpoints in any version.

14. **`14-integrations-spec.md`** — the researched integration catalog (Noto/Teachworks/TutorCruncher/classroom/MCP landscapes verified), tiered T1–T8 with phases: money (QBO+Xero native two-way), time (GCal/Outlook/ICS), the classroom-provider adapter Noto lacks entirely, consent-scoped comms, the catalog-generated Zapier/Make/n8n rail, the education-standards track, and the flagship Moyo MCP server. All of it rides the doc-13 API+webhooks — no side doors.

15. **`15-native-ai-client-spec.md`** — the Margelo/Callstack native AI stack integrated properly: adopt the chat client architecture (LegendList v3 anchored scroll, keyboard-controller, native streaming markdown, true sheets, glass chrome per our own doc-02 policy) pointed at OUR gateway; adopt on-device STT so a child's raw voice never leaves the device; prohibit on-device generation on any learner path — the Safety Plane stays server-side and fail-closed. Plus their measured perf numbers as our budget and rnsec in CI.

16. **`16-i18n-spec.md`** — cross-platform language support: use-intl shared core (official RN support) + next-intl on web with next/root-params (Next >= 16.3); per-profile locale carried in ctx; the two-axis registry rule — a language ships for learner AI only when the Safety Plane passes evals in it, fail-closed per-locale; en -> es -> sw/fr rollout, expansion budgets, RTL readiness, human-reviewed child-facing catalogs.

17. **`17-interaction-quality-spec.md`** — optimistic coordination + layout transitions: the Scharff useActionState-queue + useOptimistic-reducer patterns made cross-platform through the Block (same reducers and hooks on web and native), the pending-changes provider on the schedule vertical, band-voiced rollback for learner surfaces, the when-to-use-a-query-library boundary, ViewTransition adopted eyes-open behind the flag on ops routes (the startTransition seam drives both correctness and motion), and Reanimated layout transitions natively — one tokenized motion system.

18. **`18-tutor-ai-stack.md`** — ADR-018, the model architecture: Claude is the tutor brain; Gemini paired (not competing) in lanes — per-cell routing by eval evidence, Gemini Live for Phase-3 realtime voice, failover only within the eval-passed set; ExecuTorch as the verified edge nervous system (Whisper+VAD closes the Android STT question, multilingual Kokoro, OCR/vision, capability manager); and the education-quality gate: tutorCapabilities[subject][band] ships fail-closed per cell across elementary, junior-high and high-school bands.

19. **`19-learning-outcomes-spec.md`** — the learner loop (the system learns the child, never the model weights: mastery tracing, misconception profiles, spaced review, the per-turn learner brief), company analytics with governance (learning velocity over screen-time, k-anonymity, no individual staff dashboards), and the district reporting product: dosage/utilization, growth with external anchoring and AGS passback, equity subgroup views, the board pack, and the ESSA evidence ladder (Tier 4 at launch, standing Tier 3 annual study — Khan's published playbook as the bar).

20. **`20-build-optimization-spec.md`** — small builds on all three platforms: Callstack's and Margelo's agent skills wired into the repo (react-native-best-practices with its bundle-*/R8/app-size references, Margelo's selector, Vercel's react skills for web) with a CLAUDE.md rule making agents optimize from them; per-platform size budgets enforced by CI ratchet (AAB/R8/ABI trim, iOS thinning/strip, use-client boundary audit + dynamic islands on web, Expo Atlas snapshots, thin-barrel rule, zero bundled model weights reaffirmed); and react-native-runtimes adopted narrowly — the Natalie session surface on its own prewarmed Hermes runtime.

21. **`21-bun-gauntlet-spec.md`** — two verdicts: gauntlet-loop adopted for taste surfaces (named/fetchable quality bars, blind harsh-critic loops for learner screens, marketing, the board pack — with the Moyo rule that bars must pass our own child-safety constraints) but not for migrations; and the pnpm→Bun 1.4 conversion, phased behind gate zero (the isolated-installs+catalog dedup bug repro from oven-sh/bun#23615 must pass on pinned 1.4 or we stay on pnpm), with the migration run as a deterministic CI-judged gauntlet and hoisted-linker fallback explicitly ruled out.

22. **`22-reporting-charts-spec.md`** — the visualization stack: Victory Native (Skia, New-Arch-required, same three deps we already carry) as the general native chart layer; margelo/react-native-graph adopted as a scalpel for the guardian mastery-growth scrub line, ops revenue line, and roster sparklines; custom Skia for kid-facing progress (celebrations, not charts) and the class heatmap; Recharts on web with the board pack as a printed route (one source of truth); the typed suppression rule (suppressed cells render as explicit not-shown states, never zero); shared ChartSeries contract with pure bun-testable transforms.

23. **`23-crm-spec.md`** — the Operations Cloud CRM buildout: typed object model with the LearnerRef wall (CRM holds relationship/billing/attendance, never learning content), trial-centric pipeline with saved views and activity timeline, the one-event-catalog automation engine with the mastery-reference ban enforced at the schema level, a constraint-solver scheduling assist meeting Tutorbase's Find-Slot bar, business-signals-only health scoring, and CSV importers from Teachworks/TutorCruncher/TutorBird as the switching lever — grounded in 2026 market research where TutorCruncher is the pipeline benchmark and the CRM gap in TutorBird/Noto-class tools is the opening.

24. **`24-homework-capture-spec.md`** — the homework camera flow: five steps reconciling Photomath's scan-window and Socratic's crop-after models around the single-question isolation rule; a realtime coaching layer built strictly on Margelo's VisionCamera v5 skill (state-only hints on benchmarked ANE/NPU backends, frame-coupled Skia edge overlay, warm-once/bounded-async invariants, ≤2s shutter-to-first-token); crop-as-privacy (full frame never leaves device, EXIF stripped, on-device redaction, transcript TTL); band-adapted UX from elementary auto-capture to HS batch; and the handoff where Natalie coaches instead of vending answers.

25. **`25-lms-interop-spec.md`** — the two-lane LMS architecture: LTI 1.3/Advantage (launch context, NRPS roster, Deep Linking, AGS grade passback — no second login) plus the Content Connector (Canvas-first provider APIs, sync-then-retrieve, never live-query per question); hardened so the answer-key wall is an index boundary (student-tutor index built only from aiUsage:'studentTutor', CI-checked), one session bootstrap merged into the learner brief with guided-only non-negotiable, AI scores non-authoritative by default, platform-owned billing/guardian/learning-profile with no LMS write path, and permission-filtered retrieval before similarity.
26. **`26-realtime-collab-spec.md`** — the Tutor Room's three planes: Yjs/Hocuspocus self-hosted as the state plane (whiteboard, homework annotations from doc-24 captures, shared notes; awareness ephemeral, never persisted), Fishjam as the media plane (AV/screenshare; hosted platform, RN/Expo SDK; its server-SDK Gemini Live agent lane feeds ADR-018 Phase 3), Payload as the one authz truth minting both tokens; Natalie as a server-authored collaborator whose every op passes the Safety Plane; strokes are learner content under TTL/erasure/S27; staged so Yjs ships now for AI sessions while rented rooms carry human AV until the owned-room pilot.

27. **`27-shipaton-plan.md`** — RevenueCat Shipaton 2026 battle plan: the real deadline is Sep 30 (not the self-imposed Monday, which stays as the *store submission* target because Grand Prize shortlisting runs on RevenueCat revenue during the period and "Early and Effective Release" is a named criterion); the shippable slice (Homework Coach: capture -> guided coaching -> progress -> paywall, with 3D/ExecuTorch/LMS/CRM/collab all cut); seven-category entry strategy led by #BuildInPublic ($30k, audience size explicitly irrelevant) and OneSignal ($25k, one campaign suffices); sponsor map showing Expo/Software Mansion/Sentry/Mobbin already in the stack; and the day-by-day to Aug 31.

28. **`28-ops-dashboard-spec.md`** — the ops dashboard data architecture: Query owns server state, Table owns headless logic, Virtual owns windowing, Pacer owns execution timing, Zustand owns durable view prefs only — plus the four overlap traps (Zustand mirroring server data, debouncing the fetch instead of the input, table state that belongs in the URL, and Query mutations colliding with doc 17's reducer write path). Includes the maturity read (Table v9 tree-shaking vs Pacer beta / Store alpha), the own-Next-route-not-Payload-view decision, and the DataTable primitive.

29. **`29-bunny-media-spec.md`** — Bunny.net as the media host: the two-product split (Storage + Optimizer for images/documents, Stream for video because Storage won't transcode), the existing payload-storage-bunny adapter to evaluate rather than rebuild, direct client-to-Bunny uploads where the server only mints short-lived credentials (TUS presigned for video, signed URLs for storage), the two-phase progress model that fixes the bar-hits-100%-but-nothing-plays bug, replace-don't-overwrite media records, and token-authenticated delivery with TTL and erasure cascade for learner captures.

30. **`30-upload-surfaces-spec.md`** — the file-input experience: three components not one (ReplaceTarget where the avatar/logo itself is the drop target, Dropzone with the rules printed inside it, detached TransferTray that survives navigation), validation on drop with per-file inline fixes and a count-accurate primary button, per-file progress/retry/remove rather than batch-level anything, the full state list including page-level drag and at-limit, a11y where most dropzones fail (real file input, keyboard, live region, no drag instructions on touch, paste support), and react-dropzone as headless UI over doc 29's Bunny transport.

31. **`31-grade-voice-safety-incidents.md`** — the fix for the tutor talking to a 1st grader like a graduate seminar (which is the measured default: LLMs answer at 10th-grade+ unprompted, one study clocked Claude lesson plans at FKGL ~19.9): four bands K-2/3-5/6-8/9-12 enforced by metric-guided frames, graded few-shots, a post-generation readability gate (Spache/Dale-Chall/FK by band, rewrite-don't-block, target-vocabulary exemption so teaching new words stays legal), and reading-level eval cells in the doc-18 registry. Plus the child-input guardrail ladder S1-S4 (cursing and sexual content reach the guardian as Incident Reports; self-harm/abuse get fixed human-written scripts, crisis resources, human paging, never generated text), and the IncidentReports CMS system — automated + human-submitted, full lifecycle with append-only audit trail, guardian acknowledgment loop, SLA paging, Teams/Teachable/Circle-patterned surfaces under doc 08 hierarchy law (no red walls on intake; redpen only where something actually crossed a line).

32. **`32-tutor-voice-tone.md`** — one ElevenLabs voice everywhere (same voice ID in chat playback and the 3D embodiment; degraded mode is text-only, never a substitute voice), routed across two render paths because v3 (GA Mar 2026, audio tags, explicitly not realtime) and Flash v2.5 (75ms, no tags, emotion from text) split quality and latency: Flash streams every live turn on the doc-07 sentence window with previous_text prosody stitching, v3 renders the baked set pieces (greetings, celebrations, all S4 safety scripts) through the now-open-source Audio2Face-3D + Audio2Emotion into ARKit blendshape clips with emotion explicitly specified so face and voice can't disagree. Tone is a closed, versioned 9-entry pedagogical palette emitted as structured metadata (never inline — Flash speaks tags aloud), band-modulated, with two hard lines: no intimacy tones exist, and tone responds to lesson state never to the child's affect (A2E runs on Natalie's output audio only). Gemini Live's native voices are superseded; realtime = STT → LLM → Flash.

33. **`33-moyo-learn-prd.md`** — the standalone product requirements document for Moyo Learn: one-liner and vision, the four problems (answer-vending AI with the 3x-guided evidence, graduate-level reading defaults, retrofitted safety amid the FTC/SB-243/COPPA climate, fragmented human tutoring at $151B market scale), competitive positioning (Photomath's camera with Khanmigo's conscience plus band voice, embodied one-voice Natalie, guardian trust layer, and the Operations Cloud nobody else has), personas by band, ten binding product principles, fourteen feature areas with numbered testable FRs mapped to pack docs, nine explicit non-goals (no social features, no voice input v1, no answer mode ever), NFRs, the metric law (mastery up while time-in-app down = success), M0-through-Phase-3 rollout anchored on Shipaton, risk table led by free-ChatGPT and price-anchor pressure, open questions, and ten v1 acceptance criteria including the monthly S4 drill and the erasure test.

Build order: doc 11's PR-26 (agent surface + CLAUDE.md) lands in Wave 1 before other work; doc 09 supersedes the PR sequencing in docs 03/05/06 — screens ship against the mock session first; auth lands in Wave 3.

Where documents disagree, the later one wins: security spec > auth spec > monetization spec > briefs > tailoring > design spec > plan (the tailoring doc lists each supersession explicitly).

## Where to start — day one

**PR-0 (an afternoon):** product scope rename; fix the README font drift; add semantic color aliases (`highlighter`, `ballpoint`, `redpen`, `grade`) in `packages/theme/tokens.ts` so renamed scales never leak into feature code.
**PR-1 (day one-two):** the hot/cool dial axis in tokens + `build-css.mjs`; Storybook page rendering every component at both temperatures; automated contrast check over every emitted pair. This makes the design language executable before any new screen exists.

**Week one:** PR-2 (Better Auth: `auth` PG schema, orgs/memberships/guardianship, context switcher in the drawer) and PR-3 (`domain-services` + first platform collections). **Week two-three:** PR-4/5 — schedule persistence + `validate.ts` + the disclosure ladder on the split view. That sequence makes **S1 (Ops Resource Schedule)** the first fully-realized screen — deliberately, because the research says it's the screen that wins paying businesses, and paying businesses are the distribution channel that puts the AI tutor in front of children.

**In parallel, non-code:** run the 2-week research sprint from plan §2.8 (owners, tutors, parents). Its exit criteria are already wired into the briefs — S2's <60s Find-a-Time test is the first acceptance gate.

## The gates (every PR, no exceptions)

`turbo typecheck` green · no invented APIs (cite installed source) · new deps enter through the catalog with versions resolved at install · generated output committed · stories for every new component · contrast check green · screen work matches its brief · the §9 roster header on every agent prompt.

## The two numbers that matter

**Weekly engaged-learner rate** (the anti-15%) and **next-item correctness** (did the child actually get stronger). Every screen brief names which of these it serves. If a feature moves neither, it waits.
